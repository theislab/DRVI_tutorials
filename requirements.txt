# Dependencies for the DRVI tutorials.
#
# This is a curated list of the packages the tutorials import directly. Their
# transitive dependencies (torch, lightning, umap-learn, numpy, pandas, ...) are
# pulled in automatically by drvi-py / scvi-tools / scanpy.
#
# NOTE: The LLM-based factor-identification notebook
# (identification_of_factors_3_llm_tools) needs extra packages (anndict, CASSIA,
# gs2txt and an LLM backend such as Ollama). Those are intentionally NOT listed
# here — install them separately via the install cell inside that notebook.

# Core DRVI stack
drvi-py==0.2.6
scvi-tools==1.4.3
scanpy==1.12.2
anndata==0.12.19

# Cell-type annotation (notebook 1)
celltypist==1.7.1

# Biological-process enrichment (notebook 2)
gseapy==1.3.0
gprofiler-official==1.0.0
decoupler==2.1.6
statsmodels==0.14.6

# Plotting / analysis helpers
networkx==3.6.1
seaborn==0.13.2
matplotlib==3.10.9
openpyxl==3.1.5
requests==2.32.5
h5py==3.16.0

# Notebook environment
jupytext==1.19.3
jupyterlab==4.5.6
ipykernel==7.2.0
